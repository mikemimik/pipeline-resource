#! /usr/bin/env node
'use strict';

const readline = require('readline');

const read = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

read.on('line', (input) => {
});

read.on('close', () => {
  process.exit(0);
});
